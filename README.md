For complete dotfile solution:
https://github.com/terlar/dotfiles

## Installation
```
git clone git://github.com/terlar/zshfiles.git ~/.zshfiles
ln -s ~/.zshfiles/zsh ~/.zsh
ln -s ~/.zshfiles/zshenv ~/.zshenv
ln -s ~/.zshfiles/zshrc ~/.zshrc
```
